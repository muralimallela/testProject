// Initialize Typed.js
var options = {
    strings: ["Welcome to the Student Project Management Portal"],
    typeSpeed: 40, // typing speed in milliseconds
    backSpeed: 25,
    showCursor: false, // hide the cursor
    loop: false, // loop the animation
};

var typed = new Typed(".typing-text", options);